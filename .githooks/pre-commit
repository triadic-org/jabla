#!/usr/bin/env bash
# jabla pre-commit hook. Install with: make hooks
# Bypass once with: git commit --no-verify
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

echo "[pre-commit] make check (ascii + clj-kondo + C++ headers)..."
make -s check

echo "[pre-commit] ok"
